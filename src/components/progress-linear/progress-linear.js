import {BaseComponent} from '../../core/base-component';

export class ProgressLinear extends BaseComponent {
    constructor() {
        super();
    }

    /**
     * CreatedCallback
     *
     * @override
     * The behavior you define occurs
     * when the element is registered.
     */
    createdCallback() {
        super.createdCallback();
    }
}
